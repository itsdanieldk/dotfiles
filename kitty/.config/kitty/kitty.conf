# ============================================================
# Font
# ============================================================
font_family FiraCode Nerd Font Mono

bold_font auto
italic_font auto
bold_italic_font auto

font_size 14.5
macos_thicken_font 0

disable_ligatures never


# ============================================================
# Text / Shell
# ============================================================
term xterm-kitty

shell_integration enabled
allow_hyperlinks yes
editor nvim


# ============================================================
# macOS
# ============================================================
# 'left' sends Alt for left-option (needed by Alt-keyed widgets like fzf's
# Alt-C directory jump) while right-option still types composed characters.
macos_option_as_alt left
macos_titlebar_color background


# ============================================================
# Window
# ============================================================
enabled_layouts splits,stack

window_padding_width 8

background_opacity 0.90
background_blur 20

remember_window_size yes

hide_window_decorations no


# ============================================================
# Behavior
# ============================================================
copy_on_select clipboard

scrollback_lines 50000

cursor_blink_interval 0

enable_audio_bell no

confirm_os_window_close 2


# ============================================================
# Tabs
# ============================================================
tab_bar_style powerline

tab_title_template "{index}: {title}"


# ============================================================
# Keyboard (macOS conventions)
# ============================================================

# Terminal actions
map cmd+k clear_terminal to_cursor active

# Splits
map cmd+d       launch --location=vsplit
map cmd+shift+d launch --location=hsplit

# Close
map cmd+w       close_tab
map cmd+shift+w close_window

# Tabs
map cmd+t       new_tab
map cmd+1       goto_tab 1
map cmd+2       goto_tab 2
map cmd+3       goto_tab 3
map cmd+4       goto_tab 4
map cmd+5       goto_tab 5
map cmd+6       goto_tab 6
map cmd+7       goto_tab 7
map cmd+8       goto_tab 8
map cmd+9       goto_tab 9

# Font size
map cmd+equal change_font_size all +1.0
map cmd+minus change_font_size all -1.0
map cmd+0     change_font_size all 0

# Ligatures
map cmd+shift+u       disable_ligatures_in active always
map cmd+shift+alt+u   disable_ligatures_in active never

# Scrollback search
map cmd+f launch --type=overlay --stdin-source=@screen_scrollback /usr/bin/less +G -R

# Navigate between splits
map cmd+left  neighboring_window left
map cmd+right neighboring_window right
map cmd+up    neighboring_window up
map cmd+down  neighboring_window down

# Layout
map cmd+shift+l toggle_layout stack

# Reload config
map cmd+shift+r load_config_file


# ============================================================
# Theme
# ============================================================
include themes/catppuccin-frappe.conf
