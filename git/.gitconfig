# ============================================================
# Identity
# ============================================================
# Personal default. Repos under ~/work/ are overridden by the [includeIf] at
# the bottom of this file, which must stay after this block to take effect.
[user]
    email = hey@itsdaniel.dk
    name = itsdanieldk
[credential]
    helper = osxkeychain


# ============================================================
# Core & pager
# ============================================================
# delta is the pager. This [delta] section is the single source of truth —
# lazygit invokes bare `delta`, which reads these settings itself.
[core]
    editor = nvim
    pager = delta
    fsmonitor = true
    untrackedCache = true
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true
    side-by-side = true
    line-numbers = true
    syntax-theme = Catppuccin Frappe


# ============================================================
# Workflow defaults
# ============================================================
[init]
    defaultBranch = main
[pull]
    rebase = true
[push]
    autoSetupRemote = true
    followTags = true
[commit]
    verbose = true
[fetch]
    prune = true
    prunetags = true
    fsckobjects = true


# ============================================================
# Object integrity
# ============================================================
# Verify objects on the way in and out; catches corruption and some classes of
# malicious history. Costs a little time on large fetches.
[transfer]
    fsckobjects = true
[receive]
    fsckobjects = true


# ============================================================
# Merge & rebase
# ============================================================
[merge]
    conflictstyle = zdiff3
[rerere]
    enabled = true
    autoupdate = true
[rebase]
    autoStash = true
    updateRefs = true


# ============================================================
# Diff & display
# ============================================================
[diff]
    algorithm = histogram
    colorMoved = default
[grep]
    lineNumber = true
[log]
    follow = true
[blame]
    markUnreachableLines = true
[help]
    autocorrect = prompt
[branch]
    sort = -committerdate
[tag]
    sort = -version:refname
[column]
    ui = auto


# ============================================================
# Aliases
# ============================================================
[alias]
    lg = log --graph --oneline --decorate --all
    last = log -1 HEAD --stat
    undo = reset --soft HEAD~1
    today = "!git log --since=midnight --author=\"$(git config user.name)\" --oneline"
    amend = commit --amend --no-edit
    recent = branch --sort=-committerdate --format='%(committerdate:relative)%09%(refname:short)'


# ============================================================
# Conditional includes
# ============================================================
# Work identity for any repo under ~/work/, overriding the [user] block above.
# Must stay last. 'gitdir/i' is the case-insensitive form — required on macOS,
# where the filesystem is case-insensitive and ~/Work would otherwise miss.
[includeIf "gitdir/i:~/work/"]
    path = ~/.config/git/config-work
